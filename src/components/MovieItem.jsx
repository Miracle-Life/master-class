import React from "react";export default class MovieItem extends React.Component {    constructor(props) {        super(props);        this.state = {            willWatch: false        }    }    render() {        const {movie, removeMovies, addMovieToWillWatch, removeMoviesFromWillWatch} = this.props;        return (            <div className="card mb-4">                <img                    className="card-img-top"                    src={`https://image.tmdb.org/t/p/w500${movie.backdrop_path ||                    movie.poster_path}`}                    alt=""                />                <div className="card-body">                    <h6 className="card-title">{movie.title}</h6>                    <div className="d-flex justify-content-between align-items-center">                        <p className="mb-0">Rating: {movie.vote_average}</p>                        <button type="button"                                className={this.state.willWatch ? "btn btn-success" : "btn btn-secondary"}                                onClick={() => {                                    this.setState({                                        willWatch: !this.state.willWatch                                    })                                    {                                        this.state.willWatch ? removeMoviesFromWillWatch(movie) : addMovieToWillWatch(movie)                                    }                                }}                        >                            Will Watch                        </button>                    </div>                    <button className="btn btn-outline-danger" onClick={removeMovies.bind(this, movie)}>                        Delete movie                    </button>                </div>            </div>        )    }}